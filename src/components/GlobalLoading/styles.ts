import styled from "styled-components/native";

export const FullSizeWrapper = styled.View`
    position: absolute;
    left: 0;
    top: 0;

    align-items: center;
    justify-content: center;

    width: 100%;
    height: 100%;

    background-color: rgba(0,0,0,0.4);
    z-index: 1;
`